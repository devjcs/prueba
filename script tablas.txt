CREATE TABLE "PREPROD_GODESA"."EXAMEN" 
   (
   "IDEXAMEN" NUMBER GENERATED ALWAYS AS IDENTITY (START WITH 1 INCREMENT BY 1) NOT NULL,
   "DESCRIPCION" VARCHAR2(20 BYTE), 
 "COLUMN1" VARCHAR2(20 BYTE), 
 "COLUMN2" VARCHAR2(20 BYTE), 
 "COLUMN3" VARCHAR2(20 BYTE), 
 "COLUMN4" VARCHAR2(20 BYTE), 
 "RESPUESTA" NUMBER
   )TABLESPACE USERS;
   CREATE UNIQUE INDEX GESTOROPERACION.IX_EXAMEN1 ON GESTOROPERACION.EXAMEN
    (IDEXAMEN)
    TABLESPACE USERS
    NOLOGGING ;
    ALTER TABLE GESTOROPERACION.EXAMEN ADD (
     CONSTRAINT "PK_IDEXAMEN" PRIMARY KEY ("IDEXAMEN")
     USING INDEX);
     CREATE SEQUENCE PK_IDEXAMEN START WITH 1;


  CREATE TABLE "PREPROD_GODESA"."ESTUDIANTE" 
   (
   "IDESTUDIANTE" NUMBER GENERATED ALWAYS AS IDENTITY (START WITH 1 INCREMENT BY 1) NOT NULL,
   "NOMBRE" VARCHAR2(20 BYTE), 
 "EDAD" VARCHAR2(20 BYTE), 
 "CIUDAD" VARCHAR2(20 BYTE), 
 "ZONAHORARIA" VARCHAR2(20 BYTE)
   )TABLESPACE USERS;
   CREATE UNIQUE INDEX GESTOROPERACION.IX_ESTUDIANTE1 ON GESTOROPERACION.ESTUDIANTE
    (IDESTUDIANTE)
    TABLESPACE USERS
    NOLOGGING ;
    ALTER TABLE GESTOROPERACION.ESTUDIANTE ADD (
     CONSTRAINT "PK_IDESTUDIANTE" PRIMARY KEY ("IDESTUDIANTE")
     USING INDEX);
     CREATE SEQUENCE PK_IDESTUDIANTE START WITH 1;